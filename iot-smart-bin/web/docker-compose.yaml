version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - db-data:/app/data
    environment:
      DATABASE_URL: "file:/app/data/db.sqlite3"

volumes:
  db-data: